package shinhands.com.mapper;

// import org.apache.ibatis.annotations.Insert;
// import org.apache.ibatis.annotations.Param;
// import org.apache.ibatis.annotations.Select;
// import org.apache.ibatis.annotations.Update;

import java.util.List;

public interface MemeberMapper {
    // @Select("SELECT * FROM Member WHERE email = #{email}")
    // List<Member> getPerson(@Param("email") String email);

    // @Insert("Insert into Member values (#{email}, #{name}, #{pnumber})")
    // int setPerson(Member user);

    // @Update("Update Member set name = #{name} where email = #{email}")
    // Member updatePerson(Member user);

    // @Select("SELECT * FROM Member")
    // Member getAllPeople();
}